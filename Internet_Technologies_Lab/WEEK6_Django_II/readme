create new blank django web project -> check the place in same directory checkbox -> 
create virtual environment -> change import modules in urls.py accordingly -> 
add views only if creating in same app and exclude from . import views if not working in the same app 

now follow same procedure for app creation 
